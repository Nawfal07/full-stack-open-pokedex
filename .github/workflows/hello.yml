name: Hello World

on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello
        run: |
          echo "Hello world!"
      - name: Display date
        run: |
          date
      - name: Display current dir
        run: |
          ls -l
